using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Knights.Enums
{
    public enum TypeOfFighter
    {
        Player,
        Enemy,
    }

    public enum ItemTypes
    {
        None,
        Consumable,
        Shield,
    }

    public enum EnemyTypes
    {
        None,
        Human,
        Alien,
    }

    public enum QuestTypes
    {
        Collect,
        Kill,
    }
}
